const { SlashCommandBuilder, EmbedBuilder, MessageFlags } = require('discord.js');
const { ajustarSaldoAdmin, obterCarteira } = require('../store');
const { formatarDinheiro } = require('../utils/money');
function admin(i){return i.member?.roles?.cache?.has(process.env.HYDRA_ADMIN_ROLE_ID);}
module.exports={data:new SlashCommandBuilder().setName('ajustar-saldo').setDescription('Define o saldo de uma carteira com auditoria').addUserOption(o=>o.setName('usuario').setDescription('Cliente').setRequired(true)).addNumberOption(o=>o.setName('novo-saldo').setDescription('Novo saldo em reais').setMinValue(0).setRequired(true)).addStringOption(o=>o.setName('motivo').setDescription('Motivo do ajuste').setRequired(true)),async execute(i){if(!admin(i))return i.reply({content:'Você não tem permissão.',flags:MessageFlags.Ephemeral});const u=i.options.getUser('usuario',true),novo=i.options.getNumber('novo-saldo',true),motivo=i.options.getString('motivo',true);const antigo=obterCarteira(u.id).saldo;const c=ajustarSaldoAdmin(i.user.id,u.id,novo,motivo);return i.reply({embeds:[new EmbedBuilder().setColor('#F59E0B').setTitle('Saldo ajustado').addFields({name:'Cliente',value:`${u}`},{name:'Saldo anterior',value:formatarDinheiro(antigo),inline:true},{name:'Novo saldo',value:formatarDinheiro(c.saldo),inline:true},{name:'Motivo',value:motivo}).setTimestamp()],flags:MessageFlags.Ephemeral});}};
