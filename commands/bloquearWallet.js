const { SlashCommandBuilder, EmbedBuilder, MessageFlags } = require('discord.js');
const { criarOuAtualizarPerfil, definirStatusPerfil } = require('../store');
function admin(i){return i.member?.roles?.cache?.has(process.env.HYDRA_ADMIN_ROLE_ID);}
module.exports={data:new SlashCommandBuilder().setName('bloquear-wallet').setDescription('Bloqueia ou desbloqueia uma carteira').addUserOption(o=>o.setName('usuario').setDescription('Cliente').setRequired(true)).addStringOption(o=>o.setName('acao').setDescription('Ação').setRequired(true).addChoices({name:'Bloquear',value:'blocked'},{name:'Desbloquear',value:'active'})).addStringOption(o=>o.setName('motivo').setDescription('Motivo').setRequired(true)),async execute(i){if(!admin(i))return i.reply({content:'Você não tem permissão.',flags:MessageFlags.Ephemeral});const u=i.options.getUser('usuario',true),status=i.options.getString('acao',true),motivo=i.options.getString('motivo',true);criarOuAtualizarPerfil(u.id,u.username);definirStatusPerfil(i.user.id,u.id,status,motivo);return i.reply({embeds:[new EmbedBuilder().setColor(status==='blocked'?'#EF4444':'#22C55E').setTitle(status==='blocked'?'Carteira bloqueada':'Carteira desbloqueada').setDescription(`${u}\n\n${motivo}`).setTimestamp()],flags:MessageFlags.Ephemeral});}};
